---
const link = 'https://auth.riotgames.com';

const authorize = '/authorize'; //endpoint for obtaining an authorization code

const token = '/token'; // endpoint to exchange authorization codes for access, identity, and refresh tokens

const jwks_json = '/jwks.json'; //endpoint to grab JSON Web Keys for verifying the authenticity of access and identity tokens

const userinfo = '/userinfo'; //endpoint to use your access token to obtain user information
---
